#include "main.h"

/**
 * main - main code
 * @argc: pointer to the head of the list
 * @argv: pointer to argv
 *
 * Return: return exit success on success
 */

int main(int argc, char **argv)
{
lsh_loop();
return (EXIT_SUCCESS);
}
